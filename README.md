## Prediction for MNIST dataset using Neuralnetworks

I've used **Artificial Neural Networks** to fit a model for the **MNIST** dataset of *hand written digits*.

On the test set it achieves around **95% Accuracy**.

